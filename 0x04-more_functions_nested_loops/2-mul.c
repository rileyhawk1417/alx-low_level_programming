#include "main.h"

/**
 * mul - Entry point
 * @a: Int value
 * @b: Int value
 * Return:returns product of multiplication
 * Description: Program returns product after multiplying
 */
int mul(int a, int b)
{
int result = (a * b);
return (result);
}
