#include "main.h"
#include <ctype.h>

/**
 * _isdigit - Entry point
 * @c: Int value
 * Return:returns a sum of two numbers
 * Description: Program returns sum of
 * two numbers
 */
int _isdigit(int c)
{
if (isdigit(c))
{
return (1);
}
else
{
return (0);
}
}
