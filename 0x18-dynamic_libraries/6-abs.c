#include "main.h"
#include <stdlib.h>

/**
* _abs - Entry point
* @num: Input
* Return: return the absolute value
* Description: Program just returns the absolue value
*/
int _abs(int num)
{
return (abs(num));
}
